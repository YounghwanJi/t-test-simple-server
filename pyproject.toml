[project]
name = "t-test-simple-server"
version = "0.1.0"
description = "Simple server for test."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.121.2",
    "uvicorn[standard]>=0.38.0",
]
